apiVersion: v1
kind: Service
metadata:
  name: gremlin
  namespace: gremlin
  labels:
    app: gremlin
spec:
  selector:
    app: gremlin
  ports:
    - name: http
      port: 80
      targetPort: 8000
